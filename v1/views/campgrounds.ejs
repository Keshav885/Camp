<h1>This is the Campgrounds Page</h1>

<% campgrounds.forEach(function(camps){ %>
    <div>
        <li><%= camps.name %></li>
        <img style="width: 200px; margin-bottom: 5px" src="<%= camps.image %>">
    </div>
<% }); %>